export enum ProposalActionType {
    FORWARDED = "FORWARDED",
    SENT_BACK = "SENT_BACK",
    APPROVED = "APPROVED",
    REJECTED = "REJECTED",
    CREATED = "CREATED",
    EDITED = "EDITED"
}

export enum LocalBodyType {
    MC = "MC",
    NC = "NC",
    NP = "NP"
}

export enum ProposalRecommenderType {
    MLA = "MLA",
    OTHER = "OTHER"
}

export enum AreaType {
    RU = "RU",
    UR = "UR"
}

export enum UserType {
    DISTRICT = "DISTRICT",
    PLANNING = "PLANNING"
}

export enum ProposalStatus {
    ACCEPTED = "ACCEPTED",
    REJECTED = "REJECTED",
    PENDING = "PENDING"
}

export enum ProjectStatus {
    RECEIVED = "RECEIVED",
    ACCEPTED = "ACCEPTED",
    REJECTED = "REJECTED",
    PENDING = "PENDING",
    STARTED = "STARTED",
    COMPLETED = "COMPLETED"
}
